.env
.envrc
kubeconfig

# KinD kubeconfig (contains cluster certs + keys)
bootstrap/aibox-config

# OpenTofu state and provider cache
bootstrap/.terraform/
bootstrap/.terraform.lock.hcl
bootstrap/*.tfstate
bootstrap/*.tfstate.backup

# Legacy paths (in case of rename)
tofu/.terraform/
tofu/.terraform.lock.hcl
tofu/*.tfstate
tofu/*.tfstate.backup
